version: "3"
volumes:
  app:
    driver: local
services:
  snaba-front:
    image: snaba/front:1.0
    build:
      context: .
    ports:
      - "13443:13443"
      - "13501:13501"
    volumes:
      - "app:/app"
